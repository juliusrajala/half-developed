<!doctype html>
<html lang="en">

<head>
  <title>{{icon}} {{title}}</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" type="text/css" href="/css/reset.css">
  <link rel="stylesheet" type="text/css" href="/css/defaults.css">
  <link rel="stylesheet" type="text/css" href="/css/styles.css">
  <link rel="stylesheet" type="text/css" href="/css/blog.css">
  <script async defer data-domain="halfdeveloped.com" src="https://plausible.io/js/plausible.js"></script>

</head>

<body>
  <header class="hdcom-siteHeader">
    <img src="/assets/hdlogo.png" />
    <nav>
      {{ collections.all | eleventyNavigation | eleventyNavigationToHtml | safe }}
    </nav>
  </header>
  <main>
    <section>
      <div class="hdcom-postHeader">
        <h1>{{title}}</h1>
      </div>
      <div class="hdcom-postMeta">
        <span><i>🗓</i>{{date | dateReadable }}</span>
        <span><i>🖍</i>️ {{ author }}</span>
      </div>
      <article>
        {{ content }}
      </article>
      <h3>Recent posts</h3>

      <div style="display:flex; align-items: flex-start; flex-direction: row; margin: 2rem 0rem;">
        <img src="/assets/lore.png" alt="Julius Rajala"
          style="width: 80px;height:80px; border-radius: 100%;margin-right: 2rem;" />
        <ul>
          {%- for post in collections.highlight -%}
          <li>
            <a href="{{ post.url | url }}">{{post.data.icon}} {{ post.data.title }}</a>
          </li>
          {%- endfor -%}
        </ul>
      </div>
    </section>
  </main>
  <footer>
    Julius Rajala, 2021
  </footer>
</body>

</html>